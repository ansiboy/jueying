import type { PageData } from "../../out/runtime"

export let simple: PageData = {
    id: "simple",
    type: "Page",
    props: {},
    children: [
        { id: "image1", type: "Image", props: { url: "abc" }, children: [] }
    ]
}